compile:
  override:
    - ./git-tools unpack
test:
  override:
    - ./tree $HOME/git-tools/lib
    - ./tree $HOME/git-tools/bin
  pre:
    - cat /tmp/git-tools/install.log
  post:
    - ./git-tools test
