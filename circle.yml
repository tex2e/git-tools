compile:
  override:
    - ./git-tools unpack
test:
  override:
    - ./tree $HOME/git-tools/lib
    - ./tree $HOME/git-tools/bin
  pre:
    - cat /tmp/git-tools/install.log
  post:
    - LD_LIBRARY_PATH=$HOME/git-tools/lib ./git-tools test
